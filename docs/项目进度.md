# 项目进度总览

> 最后更新：2026-06-07

## 📊 整体进度

- **基础架构**: ✅ 100% 完成
- **数据同步**: ✅ 100% 完成（已完成故事、公示板及鸣谢名单的同步管线打通）
- **图片存储**: ✅ 100% 完成
- **文档体系**: ✅ 100% 重构完成（符合四大标准）
- **PC 端开发**: 🔄 88% 进行中（全局导航栏、全站视口画框、启封路由迁移、开屏页重构均已落地，进入联调与验收阶段）
- **移动端开发**: ✅ 100% 完成（移动端全面启用了原生列式瀑布流、响应式适配与纯净无白边大图预览，以及平滑的共享组件布局动画节奏编排修复）
- **动效系统**: ✅ 100% 完成（磁吸按钮、延迟追踪光标、SVG feColorMatrix 反色滤镜、公告弹窗联动、Luge 滚动驱动动效引擎、页脚自动吸附均已实施）
- **内容填充**: 🔄 10% 进行中（飞书鸣谢名单与基础结构已引入）
- **部署上线**: ⏳ 0% 待开始

**总体进度**: 约 90% 完成

---

## ✅ 已完成（Developer A）

### 第一阶段：项目初始化（2026-02-02）

- [x] Next.js 14 项目创建（App Router）
- [x] TypeScript 配置
- [x] Tailwind CSS v4 配置
- [x] ESLint 配置
- [x] Git 仓库初始化并连接 GitHub

### 第二阶段：核心依赖安装（2026-02-02）

- [x] Shadcn/UI 组件库
  - Button, Dialog, ScrollArea, Tooltip
- [x] Framer Motion 动画库
- [x] Zustand 状态管理
- [x] Lucide React 图标库
- [x] 中文字体配置（Alibaba PuHuiTi Medium + ZouLDFXKAJ）

### 第三阶段：基础架构搭建（2026-02-02）

- [x] 项目目录结构
  - `src/app/` - Next.js 路由
  - `src/components/` - 组件（ui, shared, desktop, mobile）
  - `src/lib/` - 工具函数和类型
  - `src/data/` - 数据文件
  - `src/scripts/` - 脚本工具
- [x] 核心类型定义（`types.ts`）
  - Story 接口
  - LocationPoint 接口
  - FeishuResponse 接口
- [x] 全局状态管理（`store.ts`）
  - 信封状态
  - 选中地点
  - 当前故事索引
- [x] 响应式检测 Hook（`hooks.ts`）
- [x] 工具函数（`utils.ts`）

### 第四阶段：页面与组件（2026-02-02）

- [x] 全局布局（`layout.tsx`）
  - 字体配置
  - 元数据配置
- [x] 主页（`page.tsx`）
  - 响应式分流逻辑
  - PC/移动端组件加载
- [x] Loading 页面（`loading.tsx`）
- [x] 404 页面（`not-found.tsx`）
- [x] 开场信封动画（`EnvelopeIntro.tsx`）
  - 信封打开动画
  - 状态管理集成
- [x] PC 端占位组件（`DesktopExperience.tsx`）
- [x] 移动端占位组件（`MobileExperience.tsx`）

### 第五阶段：数据准备（2026-02-02）
- [x] 数据结构验证
- [ ] ~~生成 Mock 数据（mock-content.json）~~ (已废弃，改用飞书同步)

### 第六阶段：飞书数据同步（2026-02-03）

**认证与 API 集成**
- [x] 飞书应用创建与配置
- [x] tenant_access_token 获取
- [x] Wiki 节点 API 集成
  - 从 Wiki URL 获取 app_token
  - 脚本：`get-wiki-app-token.ts`
- [x] Bitable 搜索记录 API
  - 支持分页
  - 支持视图过滤
  - 支持条件过滤

**表格初始化**
- [x] 创建表格字段
  - 角色ID、角色名
  - 头像URL、大图URL
  - 故事内容、投稿人、日期
  - 地点ID、地点名称
- [x] 填充示例数据（3 条记录）
- [x] 脚本：`init-feishu-table.ts`

**数据同步脚本**
- [x] 完整的 ETL 流程 (`sync-feishu.ts`)
- [x] 图片自动上传 OSS
- [x] URL 回写飞书

### 第七阶段：文档重构与标准化（2026-02-21）

- [x] **文档中文化**: 全面重命名并翻译关键文档。
- [x] **标准对齐**:
  - 修改唯一性 (SSOT)
  - 状态纯粹性 (分离 `项目进度.md`)
  - 引用替代复制 (相对路径链接)
  - 无冲突溯源 (明确定义出处)
- [x] **遗漏补全**:
  - 补充 [视觉规范](./design/视觉规范.md) (Assets Checklist)
  - 补充 [环境搭建](./guides/环境搭建.md) (分支管理与目录权限)

### 第八阶段：核心业务开发（2026-02-21）

**移动端 (Developer C)**
- [x] **StoryFeed (瀑布流列表)**: 实现双列瀑布流布局与转场动画。
- [x] **MobileDetailModal (详情页)**: 支持下拉关闭、左右切换与布局动画。
- [x] **StaticMapModal (静态地图)**: 实现地图查看与 Pin 点展示。
- [x] **MobileExperience (主入口)**: 整合子组件并处理 100vh 与滚动穿透问题。

**PC 端 (Developer B)**
- [x] **InteractiveMap (交互地图)**: 实现全屏地图适配，Pin 点定位与交互。
- [x] **StoryCardStack 优化**: 修正幽灵图片问题，优化卡片交互表现。
- [x] **资源更新**: 更换高精度 SVG 地图。

### 第九阶段：稳定性修复（2026-02-25）

- [x] 清零 `eslint` 阻断错误（P0）。
- [x] 收敛脚本/UI 层 `any`，补充关键类型定义。
- [x] 移动端手势状态由 `window` 改为 `useRef`，消除全局污染风险。

### 第十阶段：P1 工程优化（2026-02-25）

- [x] 收敛 `next/image` 的 `unoptimized` 使用。
- [x] 为地图、详情、列表、头像等场景补齐 `sizes`。
- [x] `sync-feishu.ts` 完成单文件分层重构：
  - `feishuClient`
  - `ossService`
  - `dataTransformer`
  - `fileWriter`

### 第十一阶段：PC 端交互重构与体验修复（2026-02-28）

- [x] 地图/故事切换从“图层位移”重构为“上下文档流 + 原生平滑滚动”。
- [x] 故事区增加留白缓冲，文本未展开时地图不侵入故事阅读区。
- [x] 修复快速切换地点时 `Cannot read properties of undefined (reading 'id')` 运行时错误。
- [x] StoryCardStack 在不改变动画语义前提下完成性能优化（`memo/useMemo/useCallback` 与图片预加载）。
- [x] 全局隐藏页面横向溢出，移除底部横向滚动条。

### 第十二阶段：代码审查问题修复（2026-03-05）

- [x] **Store 死代码清理**：`src/lib/store.ts` 删除未被消费的状态字段，收敛为最小状态集合。
- [x] **交互地图尺寸逻辑收敛**：`src/components/desktop/InteractiveMap.tsx` 将 `rollOffset` 与 `mapSize` 计算统一到同一 `ResizeObserver` 回调，移除重复监听。
- [x] **移动端索引计算优化**：`src/components/mobile/MobileExperience.tsx` 使用 `useMemo` 缓存 `currentLocalIndex` / `currentLocationIndex`，避免重复 `findIndex`。

### 第十三阶段：设计系统 v2 建立（2026-04-15）

- [x] **视觉规范升级**：更新 [视觉规范](./design/视觉规范.md)，确立“暖纸张 + 火漆红 + 本地中文字体”的新版视觉基线。
- [x] **字体系统落地**：`src/app/layout.tsx` 改为接入本地标题字体与正文字体，替换旧版 Google Fonts。
- [x] **全局 Token 收敛**：`src/app/globals.css` 统一维护颜色、圆角、阴影、纸张背景与基础工具类。

### 第十四阶段：协作基建与开屏下滑页联调（2026-04-17）

- [x] **React-Motion-Maker 协作资产落地**：新增 `agents/react-motion-maker/` 与 `.opencode/agents/react-motion-maker.md`，补齐动效协作所需的知识、技能与规则目录。
- [x] **开屏下滑页初版落地**：完成“关于企划 / 关于我们 / 鸣谢 / 页脚”页面群、红色引导线、页脚揭露结构与项目级 `opencode.json` 配置。
- [x] **构建链路修复**：`tsconfig.json` 排除 `agents/` 目录，解决 `npm run build` 因协作资产进入 TypeScript 编译范围导致的失败。
- [x] **开屏时序修复**：完成丝带先显影、信封 Spring 飘落、火漆碎裂、信纸居中放大与信封壳体下落的开信动画链路。
- [x] **滚动与衔接修复**：修复引导线首尾衔接、关于我们避让布局、页脚揭露层级、封盖翻转层级与过渡遮罩问题。
- [x] **平滑滚动切换**：桌面端启封态下滚页面群由自定义插值方案切换为 Lenis，实现更稳定的平滑滚动体验。

### 第十五阶段：创作公示板文档建模（2026-04-17）

- [x] **新增功能文档**：完成 [创作公示板](./design/创作公示板.md) 草案，定义 `/creation` 路由目标、版式与交互边界。
- [x] **交互与索引对齐**：将 `/creation` 独立路由同步写入 [交互设计](./design/交互设计.md) 与 [文档索引](./文档索引.md)。
- [x] **先行数据模型定义**：在 [数据模型](./architecture/数据模型.md) 中补充 `CreationIdea` 前端领域模型占位。
- [x] **飞书表结构同步**：已读取远程飞书表 `揭示板` / 视图 `收集结果` 的字段结构，并回填字段映射。
- [x] **前端实现**：`/creation` 页面与便签瀑布流组件（`src/app/creation/page.tsx`、`src/components/creation/`）已落地，页面层按 `CardID` 聚合 `CreationIdea` 为 `CreationCard`。

### 第十六阶段：创作公示板同步链路与契约收敛（2026-04-17）

- [x] **同步链路上线**：`src/lib/sync-service.ts` 打通飞书创作表 → `src/data/creation-board.json` 的 ETL 链路，参考图场景支持 OSS 上传并回填飞书 `文本` 字段。
- [x] **数据契约收敛**：`CreationEntry` 仅保留 `id / cardId / author / tags / content / images / createdAt`，移除 `title / contentType / sourceIdeaId / sourceIdeaTitle` 等衍生字段，`tags` 收敛为单个字符串。
- [x] **无图兜底规则**：`参考图` 标签下无 `images` 时由 `CreationNoteEntry` 自动回退展示 `content`，并在文档侧同步说明。
- [x] **审查报告归档**：沉淀 [代码审查报告（2026-04-17）](./review/代码审查报告-2026-04-17.md)。

### 第十七阶段：引导线 / 开信 / 滚动体验微调（2026-04-18）

- [x] **引导线起终点重构**：红色引导线起点 `P1` 对齐丝带下方的首屏底边（取消旧版 `P0(-20%)` 预留段），`P2` 下延至 `apH * 0.10`；尾段 `P5 → P6` 将 `P6` 推到 `creditsBottom + 80`，由 SVG `overflow: hidden` 蒙版统一裁剪到交界线。
- [x] **非可逆 y-aware 进度映射**：从线性滚动进度改为基于视口基准线 `refY` 的分段 anchor 策略（启动段跟视口底 → 中段锁 60% → 尾段线性过渡到 100% → 触底 snap），让描边终点始终贴合视口节奏，详见 [交互设计 §4.0](./design/交互设计.md#40-全局滚动系统)。
- [x] **开信封盖层级延迟**：封盖 `zIndex` 由 30 → 0 的切换延迟 0.15s，避免信纸放大瞬间被封盖遮挡。
- [x] **自定义滑块**：新增 `src/components/shared/CustomScrollbar.tsx`，通过 Lenis 的 `scroll` 事件驱动，与平滑滚动严格同步，hover 时由描边矩形切换为实心 `#c23643`。
- [x] **关于我们版式避让**：文案左上对齐、成员网格右下对齐，头像缩小为 `w-12/14`，为对角引导线让出中段空间。

### 第十八阶段：全局导航栏与启封路由迁移（2026-04-22 ~ 2026-04-23，PR #27 / #28）

- [x] **全局导航栏落地**：新增 `src/components/shared/GlobalNav.tsx`，胶囊式竖排导航贯通「主页 / 地图 / 公示板」三个顶层路由，常驻视口右上角；规格详见 [交互设计 §1.4](./design/交互设计.md#14-全局导航栏-global-navigation-bar)。
- [x] **地图主体验独立路由**：将地图 / 集邮册主体验从 `/` 迁出到 `/map`，`src/app/page.tsx` 专职承载启封主屏 + 下滚页面群，`src/app/map/page.tsx` 基于屏宽分流至 `DesktopExperience` / `MobileExperience`。
- [x] **启封过渡遮罩**：新增 `src/components/shared/TransitionOverlay.tsx` 持久挂在 `layout.tsx`，监听 `isTransitioning` 全屏淡出遮罩，避免 `/` → `/map` 路由切换期间页脚与地图瞬时露出。
- [x] **开信动画路由化**：`EnvelopeIntro.handleOpen` 在信纸放大结束后 `router.push('/map')`，不再依赖 `isEnvelopeOpened` 分支；挂载时统一重置 `isEnvelopeOpened` / `isIntroReady`、`opening` 阶段锁定页面滚动。
- [x] **首页开屏态附红色色块**：开屏态（`scrollY === 0`）下导航栏胶囊下方附 `28vh × 7vw` 红色长方形（仅左下角圆角），与左侧丝带横向呼应；滚动后平滑淡出。
- [x] **Strict Mode 容错**：`EnvelopeIntro` 新增 `unmountedRef`，在 setup 阶段显式置 `false`、cleanup 置 `true`，避免 Next.js 16 dev Strict Mode 下 effect 第二次 setup 后 guard 永久命中导致开信卡顿。
- [x] **#27 合并后回归修复（PR #28）**：
  - `GlobalNav` 主页按钮仅在 `pathname !== '/'` 时才重置 `isIntroReady` 并跳转，避免已在 `/` 上再次点主页后入场 effect 不重跑、页面永久锁滚。
  - `EnvelopeIntro.handleOpen` 在 `setTransitioning` / `router.push` 前用 `unmountedRef` 短路，防止用户在开封动画期间切路由后被拽回 `/map`。
  - `TransitionOverlay` 按 CLAUDE.md 硬约束补 `prefers-reduced-motion`，三点脉冲 / 文字呼吸 / 淡出在该偏好下退化为瞬态。
- [x] **手动测试回归**：`tests/manual/test-plan-global-nav-v3.md` / `test-plan-global-nav-v4.md` / `test-report-global-nav-v4.md` 沉淀导航栏三轮验收记录。

### 第十九阶段：全站视口画框（2026-04-23）

- [x] **设计文档先行**：新增 [全站视口画框](./design/全站视口画框.md)，定义 `5px #fffdfd` 外白边 + `16px` 内圆角、外角直角贴合视口、仅内缘圆角的几何契约与 Token 清单。
- [x] **全局 Token**：`src/app/globals.css` 引入 `--site-frame-border-width` / `--site-frame-radius` / `--site-frame-color` 单一事实来源；`html` 与 `body` 背景切换为画框色，`body::before` 作为纸面底板。
- [x] **共享结构类**：新增 `.site-frame-shell` / `.site-frame-border` / `.site-frame-fixed` / `.site-frame-scrollbar-track` 共享类，`layout.tsx` 将 `children` / `GlobalNav` / `TransitionOverlay` 统一挂载在 `site-frame-shell` 内，并在 `body` 末尾挂载 `site-frame-border` 作为最顶层描边层。
- [x] **几何修正**：改用 `position:fixed; inset:0; overflow:hidden` + `::before` 大 spread `box-shadow` 方案，绕开 Tailwind preflight `* { border-width: 0 }` 导致 `border` 被降为 `1px` 的冲突；最终外角直角、内角 `16px` 圆角。
- [x] **组件贴合**：`GlobalNav` 由视口边缘改为画框内缘定位，首页开屏红色色块右上角跟随画框内圆角 `11px`；`CustomScrollbar` 轨道改用 `.site-frame-scrollbar-track`，滑块位移以画框内高度（`viewport - 2×border`）为基准，不越界白边。

### 第二十阶段：开屏页重构实施（2026-04-28，PR #30）

- [x] **设计文档先行**：沉淀 [开屏页重构](./design/开屏页重构.md)，定义 10×5 网格、斜底色块、Logo / 信封中心点、双层互反与引导线遮罩的几何契约与动效时序。
- [x] **双层互反落地**：`src/components/shared/EnvelopeIntro.tsx` 重构为 Base Layer (`z-1`) + Top Layer (`z-2`) 结构：Base 为 `#ece9e4` 全底 + `clip-path: polygon(0 0, 100% 0, 100% 70vh, 0 40vh)` 的 `#c23643` 斜底色块 + CSS `mask-image` 着色的奶白 Logo；Top 通过单一内联 `<svg>` 1:1 克隆 Base 并将两种主色互换，Logo 由 `<feColorMatrix>` 重着色为 `#c23643`。
- [x] **引导线遮罩口**：Top Layer 通过 `<mask>` + 带 `stroke-linecap: round` 的 `<line>` 作为遮罩口（起点 `(60vw, 0)` → 终点 `(20vw - 50px, 100vh)`，线宽 `100px`）；起终点沿斜率方向外延 `GUIDE_OVERSHOOT = 60px` 到视口外，避免端笔在视口内露出拐点。
- [x] **入场动效时序**：阶段一 `<motion.line>` 的 `strokeDashoffset` 由 `length → 0`，`SWEEP_DURATION_MS = 1000` / `SWEEP_DELAY_MS = 120`，曲线 `cubic-bezier(0.4, 0, 0.2, 1)`；阶段二 `onAnimationComplete → setSweepDone(true)` 触发信封 Spring 飘落（`stiffness: 30, damping: 12, mass: 1.5`）到 `(75vw, 70vh)`。
- [x] **Logo / 信封几何落地**：常量 `LOGO_CENTER_X_RATIO = 0.35` / `LOGO_CENTER_Y_RATIO = 0.35` / `LOGO_WIDTH_RATIO = 0.5`，Logo 中心锚定 `(35vw, 35vh)`、横向铺幅 `50vw`；`ENVELOPE_CENTER_X_RATIO = 0.75` / `ENVELOPE_CENTER_Y_RATIO = 0.7`，信封中心锚定 `(75vw, 70vh)`（响应式像素宽度 + `marginLeft/Top: -w/2`）。
- [x] **滚动锁定全时窗覆盖**：`page.tsx` 的 `locked = mounted && !isIntroReady` 与 `EnvelopeIntro` 内部 `phase === 'opening'` 时追加的 `intro-scroll-locked` 类共同覆盖「挂载 → 扫动完成 → 信封落定 → 点击蜡封 → 路由切到 `/map`」的全时窗，避免中间任何帧露出可滚动状态。
- [x] **无障碍降级**：`prefers-reduced-motion: reduce` 下遮罩瞬时绘满、信封瞬时落定、`setIntroReady(true)` 立即放行，第一时间可点蜡封进入 `/map`。
- [x] **手动测试回归**：`tests/manual/test-plan-intro-rebuild.md` 沉淀刷新 `/` → 引导线扫动 → 信封落定 → 点击蜡封 → `/map` 的全链路测试要点（含互反色条带、引导线无拐弯、大斜底色块不跳出、滚动锁释放点等关键断言）。

### 第二十二阶段：动效组件系统（2026-05-14）

- [x] **磁吸交互按钮**：新增 `src/components/motion/MagneticButton.tsx`，实现鼠标跟随磁吸位移（1.5s Power4.easeOut）、弹性回弹（Spring 350/8/0.5）、填充层滑入滑出（76%→0%→-76%，Power2.easeInOut）与文本色彩翻转（入场 Power3.easeIn 无延迟、离场 Power3.easeOut 延迟 0.3s）。支持三种色彩方案预设与响应式磁吸断点（默认 540px）。
- [x] **高阶延迟追踪光标**：新增 `src/components/motion/StickyCursor.tsx`，以 RAF LERP 循环（外圈除数 12、内点除数 6）实现分层粘稠延迟追踪。应用 SVG `feColorMatrix` 滤镜 + CSS `backdrop-filter` 实现站点调色反色，替代 `mix-blend-mode: difference` 的通用差值混合。
- [x] **公告弹窗联动**：StickyCursor 订阅 Zustand `isAnnouncementOpen`，飞书公告弹窗开启时光标 DOM 不渲染（规避跨域 iframe 无法追踪的限制），关闭后恢复。
- [x] **全局鼠标追踪 Hook**：新增 `src/components/motion/useMousePosition.ts`，提供基于 `useMotionValue` 的全局鼠标坐标追踪，不触发 React 重渲染。
- [x] **布局挂载**：`src/app/layout.tsx` 在 `body` 顶层挂载 `<StickyCursor />`（`site-frame-shell` 之外），z-index 9999 位于画框描边层之下。
- [x] **文档同步**：新增 `docs/design/动效组件.md`，并更新文档索引、技术栈与项目进度。
- [x] **制品写作规范**：全部注释改为「现状描述」风格，禁用比较性/变更性用语。

### 第二十三阶段：Luge 动效引擎与页脚吸附（2026-06-07）

- [x] **Luge 动效引擎接入**：新增 `src/lib/useLuge.ts`，动态加载 `@waaark/luge` 全局单例，禁用内建 smooth scroll 插件以与 Lenis 解耦，其余动效能力（scroll animation、reveal、scroll observer）留待后续使用。
- [x] **页脚吸附动效落地**：新增 `src/lib/useFooterSnap.ts`，基于 Lenis `on('scroll')` 事件手控方向感知（仅向下滚动触发），配合 `lenis.scrollTo()` 以 LERP 物理驱动（`lerp: 0.16` + Expo ease-out 缓动）实现页脚自动吸附。距底部 38vh 以内 + 向下滚动时触发，停留或向上回滚不触发，回滚超过 70% 可滚动距离后重置。
- [x] **类型声明**：新增 `src/types/luge.d.ts`，为 `@waaark/luge` 提供 TypeScript 类型声明（`LugeSettings`、`ScrollObserver`、`Lifecycle`、`LugeInstance` 等接口）。
- [x] **首页集成**：`src/app/page.tsx` 接入 `useLuge(smoothEnabled)` 与 `useFooterSnap(lenis, smoothEnabled)`，与 Lenis 共享启用条件。
- [x] **文档同步**：更新 [[./architecture/技术栈|技术栈]]（新增依赖与目录条目）、[[./design/动效组件|动效组件]]（新增 §4 Luge 引擎与 §5 页脚吸附规格）、[[./design/交互设计|交互设计]]（§4.0 滚动系统补充 Luge 集成 + §4.4 页脚补充吸附行为）、[[./项目进度|项目进度]]（本条记录）。
- [x] **构件验证**：`npm run lint` / `npm run build` 均通过。

---

## ⏳ 待办事项 (To-Do)

### 基础设施 (Developer A)
- [ ] **开屏联调验收**: 在桌面与移动端复核信封开启、引导线、页脚揭露、过渡遮罩与 Lenis 平滑滚动的衔接体验。
- [ ] **部署准备**: 配置 Vercel 环境变量与构建命令。
- [ ] **同步脚本测试化**: 为 `dataTransformer` / `getText` / 分页拉取逻辑补充单测。
- [x] **创作公示板数据同步**: 已拉通飞书创作表 -> 本地 `creation-board.json` 的同步链路，并支持参考图上传 OSS 后回填 `文本` 字段。

### 第二十一阶段：便签头表同步与 Webhook 触发接口（2026-05-13）

- [x] **便签头表独立同步模块**：新增 `src/lib/sync/tables/creation-headers.module.ts`，从飞书独立头表读取 `cardId / 地点 / 角色`，写入 `src/data/creation-board-headers.json`；`creation_board` 模块在执行期直接读取该文件，无需重复调用飞书 API。
- [x] **头表注册与依赖链**：`creation_headers` 已在 `registry.ts` 注册，`creation_board.dependsOn = ['creation_headers']`，依赖解析器保障执行顺序正确。
- [x] **Webhook 触发接口**：新增 `POST /api/admin/sync/webhook`，使用 Bearer Token 鉴权（`SYNC_WEBHOOK_SECRET` 环境变量），内置 IP 限流（60 次 / 小时），专用于飞书自动化触发同步，无需 Cookie。
- [x] **类型系统结构修复**：将 `SyncTableKey` 由显式字符串联合改为 `keyof SyncTableOutputMap` 派生类型，新增同步表只需在 `SyncTableOutputMap` 追加一个键值对，`SyncTableKey` 自动跟进，彻底消除两处定义失步的风险。
- [x] **`SyncJobRecord.triggeredBy` 补全**：联合类型加入 `'webhook'`，与实际触发来源对齐。
- [x] **依赖安全升级**：Next.js 从 `16.2.4` 升级至 `16.2.6`，消除 CI 安全审计（`--audit-level=high`）报告的全部高危漏洞。

### PC 端开发 (Developer B)
- [ ] **设计系统迁移**：将 shared / desktop / mobile 组件中的硬编码 `stone` 与十六进制颜色逐步替换为语义 token。
- [x] **StoryCardStack 体验打磨**: 在现有动画语义不变前提下继续优化拖拽手感与帧稳定性。
- [x] **StoryTextArea 体验优化**: 完善文本区阅读节奏与视觉层次。
- [x] **视觉优化**: 调整地图 Pin 点动画与故事区/地图区衔接细节。

### 移动端开发 (Developer C)
- [ ] **细节验收**: 在真机环境验证 100dvh 兼容性与手势流畅度。
- [ ] **性能验收**: 进行 Lighthouse 对比并评估图片优化收益。

### 内容与运营
- [ ] **数据填充**: 在飞书多维表格中录入真实故事数据。
- [ ] **灵感录入规范**: 明确创作公示板字段填写规范、`CardID` 聚合方式与补充创意入口的使用方式。
- [ ] **素材准备**: 收集高质量的校园场景图与角色头像。
