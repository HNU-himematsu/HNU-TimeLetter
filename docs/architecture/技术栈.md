# 技术架构与技术栈

## 核心技术栈
- **Framework**: Next.js 16.1.6 (App Router)
- **Language**: TypeScript 5+
- **Styling**: Tailwind CSS v4
- **State Management**: Zustand v5
- **Animation**: Framer Motion v12（开屏、转场、卡片、滚动区块、磁吸按钮与延迟追踪光标动效）
- **Scroll Animation**: `@waaark/luge`（滚动驱动的渐进式动效引擎，smooth scroll 插件已禁用以与 Lenis 解耦）
- **Smooth Scroll**: Lenis（桌面端启封态下滚页面群；内置 Snap 用于页脚吸附）
- **UI Components**: Shadcn/UI (Radix UI) + Lucide React
- **Build Tool**: Turbopack (Next.js default)

## 外部服务
- **CMS**: 飞书多维表格 (Feishu Bitable) - 作为唯一数据源。
- **Storage**: 阿里云 OSS (Aliyun Object Storage Service) - 存储图片资源。
- **Deployment**: Vercel (推荐) / Static Export。

## 工程约束
- **TypeScript Compile Scope**: `tsconfig.json` 已显式排除 `agents/`，避免协作代理资产进入应用编译范围并影响 `npm run build`。
- **Collaboration Assets**: 根目录 `agents/` 与 `.opencode/agents/` 用于代理、技能与知识库协作，不属于前端运行时代码。
- **Local Tooling**: 根目录 `opencode.json` 用于注册项目级 Playwright MCP server，属于本地开发辅助配置，不参与线上运行。

## 目录结构规范
```text
src/
├── app/
│   ├── admin/         # `/admin` 后台页面（登录 + 受保护分组）
│   ├── api/admin/     # 后台 API（登录、手动同步）
│   ├── creation/      # `/creation` 创作公示板路由
│   ├── map/           # `/map` 地图主体验路由
│   ├── layout.tsx     # 全局布局与本地字体注册
│   └── page.tsx       # 首页（启封主屏 + 下滚页面群）
├── components/
│   ├── ui/            # Shadcn 风格基础组件（禁止直接修改逻辑）
│   ├── shared/        # 通用业务组件（EnvelopeIntro / GuideLine / CustomScrollbar / GlobalNav / TransitionOverlay）
│   ├── motion/        # 动效组件（MagneticButton / StickyCursor / useMousePosition）
│   ├── sections/      # 下滚页面群（AboutProject / AboutUs / Credits / Footer / ScrollSections）
│   ├── desktop/       # [Dev B] PC 端专用组件
│   ├── mobile/        # [Dev C] 移动端专用组件
│   └── creation/      # `/creation` 便签瀑布流组件
  ├── lib/
│   ├── admin/         # 后台鉴权 / 调度器 / 配置读写
│   ├── sync-service.ts# 飞书 + OSS 同步主服务（CLI 与 API 复用）
│   ├── types.ts       # 核心领域类型 (SSOT)
│   ├── store.ts       # Zustand 全局状态
│   ├── hooks.ts
│   ├── useVirtualScroll.ts  # Lenis 封装
│   ├── useLuge.ts           # Luge 全局初始化（smooth scroll 禁用）
│   ├── useFooterSnap.ts     # 页脚吸附（Lenis Scroll 事件 + ScrollTo）
│   └── content.ts / utils.ts
├── types/             # 第三方库类型声明（@waaark/luge 等）
├── config/            # admin.json、locations.json 等本地配置
├── data/              # 同步产物：content.json / creation-board.json
└── scripts/           # sync-feishu.ts 与各种飞书 / OSS 调试脚本
```

仓库根目录另含 `agents/`、`.opencode/agents/`、`.trae/skills/` 等协作资产目录；这些目录服务于开发流程，不纳入 `src/` 运行时代码边界。
